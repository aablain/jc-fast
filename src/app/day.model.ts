export class Day {
  constructor(public author: string, public banner: string, public date: string, public dayid: number, public devotion: string, public icon: string, public prayer: string, public scripture: string, public title: string, public verse: string, public videolink: string, public photo: string) { }
}
